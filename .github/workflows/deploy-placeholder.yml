name: Deploy Placeholder

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Azure login placeholder
        run: echo "Configure azure/login with federated credentials or a service principal."
      - name: Backend deploy placeholder
        run: echo "Deploy backend/src/Portal.Api to Azure App Service after publishing."
      - name: Frontend deploy placeholder
        run: echo "Deploy frontend to Azure App Service or Azure Static Web Apps."
      - name: Key Vault placeholder
        run: echo "Inject Azure SQL and Entra settings through App Service settings or Key Vault references."
